<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DEVA X CRICKET</title>
    <style>
        /* CSS Styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #2b1f47; /* Dark Purple/Indigo background */
            display: flex;
            justify-content: center;
            align-items: flex-start; /* Align content to the top */
            min-height: 100vh;
            color: white;
            padding-top: 20px; /* Space from the top edge */
        }

        .container {
            width: 90%;
            max-width: 400px;
            padding: 20px;
            text-align: center;
        }

        /* 1. Header Section (DEVA X CRICKET) */
        .header {
            font-size: 1.8em;
            font-weight: bold;
            margin-bottom: 25px;
            letter-spacing: 2px;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
        }

        .money-bag {
            color: gold;
            margin: 0 10px;
            font-size: 1.2em; /* Slightly larger than text */
        }

        /* 2. Profile Image */
        .profile-pic-container {
            border-radius: 50%;
            overflow: hidden;
            width: 200px;
            height: 200px;
            margin: 0 auto 30px;
            border: 5px solid white; /* White glow effect */
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.7); /* Outer glow */
            position: relative;
            background-color: red; /* To show the background behind the mask */
        }

        .profile-pic {
            width: 100%;
            height: 100%;
            object-fit: cover;
            /* In a real project, you would place the image here */
            /* For this demo, we use a placeholder background */
        }
        
        /* 3. World Record Tag */
        .match-pass-tag {
            background: linear-gradient(90deg, #f7ec00, #ffc107); /* Yellow/Orange Gradient */
            color: #333;
            padding: 10px 15px;
            border-radius: 8px;
            font-weight: bold;
            margin-bottom: 30px;
            display: inline-block;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        .world-record {
            font-size: 0.9em;
            color: #d6330a; /* Reddish star for emphasis */
            margin-left: 5px;
        }
        
        /* 4. Subscribe Button */
        .subscribe-btn {
            background: linear-gradient(90deg, #ff007f, #e83e8c); /* Pink/Magenta Gradient */
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 30px;
            font-size: 1.1em;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
            max-width: 300px;
            margin: 0 auto 40px;
            box-shadow: 0 6px 15px rgba(255, 0, 127, 0.5); /* Pink shadow */
            transition: transform 0.2s;
            text-decoration: none;
            display: inline-block;
        }

        .subscribe-btn:hover {
            transform: translateY(-2px);
        }

        .telegram-icon {
            margin-left: 10px;
        }
        
        /* 5. Disclaimer Box */
        .disclaimer-box {
            background-color: rgba
